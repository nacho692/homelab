# Homelab

Infrastructure-as-code for a two-machine homelab.

## Repository Structure

- `ansible/` — Service deployment and host configuration. See [ansible/README.md](ansible/README.md).
- `nixos/` — NixOS flake-based system configuration for the transcoder host.

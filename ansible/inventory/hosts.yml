all:
  children:
    nixos:
      hosts:
        transcoder:
          ansible_host: 10.0.1.187
          ansible_user: skynet
          ansible_connection: ssh
    deb:
      hosts:
        nas:
          ansible_host: 10.0.0.2
          ansible_user: skynet
          ansible_connection: ssh

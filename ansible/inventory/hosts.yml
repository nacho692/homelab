all:
  children:
    nixos:
      hosts:
        transcoder:
          ansible_host: 10.0.1.187
          ansible_user: skynet
          ansible_connection: ssh
    nas:
      hosts:
        nas-host:
          ansible_host: 10.0.0.2
          ansible_connection: local
